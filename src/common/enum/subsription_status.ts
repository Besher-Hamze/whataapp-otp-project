export enum SubscriptionStatus{
    PENDING="pending",
    APPROVED="approved",
    REJECTED='rejected'
}

export enum UserSubscriptionStatus {
    ACTIVE = 'active',
    WARNING = 'warning',
    EXPIRED = 'expired'
}